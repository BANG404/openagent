# Sources and development Runtime

Read `docs/release/sources.md` for release source boundaries and direct-push
rules. Read `docs/release/development-runtime.md` for development Runtime
refresh behavior. Keep private SDK implementation in the SDK repository and
the host-side adapter thin.
