# Workflow basics

Read `docs/release/conventions.md` for commit conventions and
`docs/release/local-commands.md` for local release and recovery commands.
These procedures are scoped to release work; repository delivery and commit
ownership remain governed by `deliver-via-pr`.
