interface:
  display_name: "Deliver repository changes"
  short_description: "Deliver local, PR-sync, or merged repository changes"
  default_prompt: "Use $deliver-via-pr to apply the selected local, OPR, or ORPR workflow while keeping the default worktree safe and synchronized."
