interface:
  display_name: "Deliver repository changes"
  short_description: "Choose local, PR sync, or full PR delivery"
  default_prompt: "Use $deliver-via-pr to keep local default branches in place and apply the selected local, OPR, or ORPR delivery workflow."
