interface:
  display_name: "Deliver repository changes"
  short_description: "Deliver direct or worktree repository changes"
  default_prompt: "Use $deliver-via-pr to apply the selected direct local or OWT workflow while preserving unrelated repository state."
