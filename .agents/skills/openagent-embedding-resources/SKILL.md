---
name: openagent-embedding-resources
description: Use for the bundled embedding model, provenance, versioned resource installation, checksum verification, packaging, or resource repair behavior.
metadata:
  category: release-engineering
---

# Embedding resources

Read [docs/embedding-model.md](../../../docs/embedding-model.md) for the model
source, pinned revision, byte size, checksum, and reproducibility procedure.
Read the embedding/resource portions of `docs/configuration.md` for staged
installation, activation, repair, and startup UX.

Resources are immutable and must be staged and fully verified before activation.
