# Context compaction

Read `docs/configuration.md` section `Context compaction` for threshold,
recent-event, checkpoint, truncation, and summary behavior. Keep durable
checkpoint content chronological and preserve the distinction between the
complete visible transcript and the compacted supplemental timeline.
