# Diagnostics

Read `docs/configuration.md` section `Software error collection` for local
rotation, remote export, allowlisted fields, privacy exclusions, credentials,
and runtime toggling. Model-context traces and ordinary application
diagnostics remain separate pipelines.
