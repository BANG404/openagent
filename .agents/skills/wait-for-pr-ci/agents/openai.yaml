interface:
  display_name: "Wait for GitHub Actions"
  short_description: "Wait for any pull request Actions flow to finish"
  default_prompt: "Use $wait-for-pr-ci to wait for this pull request's selected GitHub Actions checks without polling GitHub separately."
