# Codex Windows sandbox helpers

Windows development and release builds generate the setup and command-runner
executables in this directory from the immutable Codex revision pinned by the
SDK. The generated `.exe` files are intentionally ignored by Git.
